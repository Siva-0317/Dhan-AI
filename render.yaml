services:
  - type: web
    name: dhan-i-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python -m rag.seed"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    autoDeploy: false
    envVars:
      - key: GROQ_API_KEY
        sync: false
